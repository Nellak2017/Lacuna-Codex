import { Box, styled } from '@mui/material'
export const Root = styled(Box)({ display: 'flex', minHeight: '100vh', height: '100%',})
export const MainContent = styled(Box)({ width: '100%', height: '100vh', overflowY: 'auto', display: 'flex', flexDirection: 'column',})